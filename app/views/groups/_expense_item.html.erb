<!-- app/views/groups/_expense_item.html.erb -->
<div style="border-bottom: 1px solid #e2e8f0; padding: 0.75rem 0; display: flex; justify-content: space-between; align-items: center;">
  <div>
    <p style="font-weight: 600; color: #1a202c; font-size: 0.875rem;"><%= expense.description %></p>
    <p style="font-size: 0.75rem; color: #718096;">
      <%= expense.user.full_name %>
      <%= ' (You)' if expense.user == current_user %>
      • <%= expense.date.strftime("%b %d, %Y") %>
    </p>
    <p style="font-size: 0.75rem; color: #4f46e5;"><%= expense.category %></p>
  </div>
  <p style="font-weight: 600; color: #4f46e5; font-size: 0.875rem;"><%= number_to_currency(expense.amount) %></p>
</div>