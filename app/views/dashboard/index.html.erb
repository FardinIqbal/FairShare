<!-- app/views/dashboard/index.html.erb -->
<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6 sm:py-8">
  <h1 class="text-2xl sm:text-3xl font-bold text-gray-900 mb-6">Hello, <%= current_user.first_name %></h1>

  <%= render 'dashboard/summary_section' %>

  <%= render 'dashboard/quick_actions' %>

  <%= render 'dashboard/owed_amounts' %>

  <div class="mt-8">
    <%= render 'dashboard/recent_activities' %>
  </div>

  <%= render 'shared/debug_info' %>
</div>